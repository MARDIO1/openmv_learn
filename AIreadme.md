# AI AGENT工具开始工作前阅读我
* 不要浪费token

## 解释风格：
* 解释代码时，带我一步一步读代码，不要跳步
* 出现了以前没有遇到的函数，必须介绍参数
* 解释举例的时候不要出现完整或者太长的代码
* 减少礼貌性废话

## 工作风格：
* 尽可能简单的视线代码，如果没有要求，不要用复杂的算法，不要用高级方案！
* 少改原本代码框架。如果非要改，务必做出接口，并且在你修改的地方留下注释说明AIchange
* 不要出现相同功能的文件，不要出现几乎一模一样的文件然后名字不一样
* 少添加，尤其是src中的文件。
* 少写不必要的注释，少换行。

* 不允许删除非AI生成的文件
* 生成的文件必须要架构清晰，但是各种名称不宜过长
* 遇到了控制台终端相关的问题，可以中断任务问我

## 本项目：
* 用最简单的方案识别绿灯，有如下特点逐个识别：1.绿色 2.亮度 
* 识别到后在屏幕上标注出来，
* 并在控制台输出识别结果，需要输出：1.识别到的数量 2.FPS 3.相对视角xy坐标
* 只用一个.py文件实现，阈值用最简单的常数